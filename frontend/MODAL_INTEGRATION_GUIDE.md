# Инструкция по обновлению модалки прихода

## Шаг 1: Добавьте CSS файл в index.html

В файле `frontend/index.html` после строки 11 добавьте:
```html
<link rel="stylesheet" href="income_modal.css">
```

Должно получиться:
```html
<link rel="stylesheet" href="analysis.css">
<link rel="stylesheet" href="income_modal.css">
</head>
```

## Шаг 2: Замените модалку в index.html

Найдите блок модалки (строки 105-122):
```html
<!-- Модальное окно для добавления прихода -->
<div id="income-modal" class="modal" style="display:none;">
    ...
</div>
```

Замените его содержимым файла `income_modal_snippet.html`

## Шаг 3: Добавьте JavaScript

В конец файла `frontend/app.js` (перед закрывающей скобкой или в самый конец) добавьте содержимое файла `income_modal_script.js`

## Что получится:

✅ **Тип операции** - выпадающий список (Приход / Долг / Возврат)
✅ **Фото превью** - квадратик 120x120px с превью
✅ **Кнопка удаления фото** - появляется при hover
✅ **Выбор валюты** - UZS, USD, EUR, RUB, CNY
✅ **Красная сумма** - для операций "Возврат"
✅ **Office стиль** - все поля в стиле Microsoft Office 2025

## Файлы для интеграции:

1. `income_modal.css` - уже создан ✅
2. `income_modal_snippet.html` - HTML код модалки
3. `income_modal_script.js` - JavaScript для работы с фото

## Быстрая интеграция (копировать-вставить):

### В index.html (строка 11):
```html
<link rel="stylesheet" href="income_modal.css">
```

### В index.html (строки 105-122 - заменить полностью):
Скопируйте содержимое `income_modal_snippet.html`

### В app.js (в конец файла):
Скопируйте содержимое `income_modal_script.js`

Готово! 🎉
